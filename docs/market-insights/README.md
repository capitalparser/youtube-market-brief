# Market Insights Public Page

This directory contains the sanitized GitHub Pages build of the private Market Insights mobile reading app.

Rules:

- Publish only `index.html`.
- Do not copy raw captures, transcripts, sidecars, or source Markdown cards into this directory.
- Keep this page as a generated artifact from the private vault.
- The public page is a synthesized reading surface, not investment advice.
- GitHub Pages deploys only this directory via `.github/workflows/pages.yml`.
